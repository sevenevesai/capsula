# Fixture

**Version**: ChatGPT Export v1.5.0

---

## You

Yes proceed with 1-3, but I think we're in phase 3 now as I have global login, multi shard and event orchestrator to automate new shard setup and starts, etc.

So maybe now its time we gave love to the RC, revamping its looks entirely. I envision the activity pings going to a chattable interface. So in a scroll area like now but more like a chat interface, with a box to submit chat messages or commands to run via the RC with permissions/roles considered of course. That way all those on RC together can also use it to communicate, or see server activity. WE simply ping RCs equally for updates/ info and RC-to-RC messages, etc. Implementing the command layer with "/" is just icing on the cake.

We'll ideally need to reorganise and think of (or force claude code to think of) clean layouts to house all we need in RC now.

We will need a button or are to open a file manager, with folders for the server editable by the user with RC to add folders, delete them, drag drop content into them, see content, rename, move files between folders, etc.

We will also need a code manager area. On graal, they have DB NPCs for hosting like highlevel aggregate stats or "all the time online" NPCs. Then weapon NPCs which are scripts directly attached to players, and then and only then ran on that player to host anything from GUIs to vehicle logic to effects to actual guns/weapons. And then level NPCs that execute at certain states (level (Re)loaded, player entered, etc). Classes for reusable scripts library you join to other scripts. I think, for now, we simply make it known we want this button in the "button area" for consideration in the new design, and we fully scope what this means later
